# ============================================
# limetax Atlas Environment Variables
# ============================================
# Copy this file to .env.local and fill in the values
# Never commit .env.local to version control!

# ============================================
# Supabase Configuration
# ============================================
# These keys are generated when you run `sh ./utils/generate-keys.sh`
# in the supabase-project/ folder. Find them in supabase-project/.env

# Supabase API URL
NEXT_PUBLIC_SUPABASE_URL=http://localhost:8000

# Anonymous/public key (safe to expose in browser)
# Location: supabase-project/.env → ANON_KEY
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key (server-side only, NEVER expose in browser!)
# Location: supabase-project/.env → SERVICE_ROLE_KEY
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================
# Anthropic (Claude AI)
# ============================================
# Get your API key from: https://console.anthropic.com/
# Ask team lead if you need access
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# ============================================
# OpenRegister (German Company Register)
# ============================================
# Get your API key from: https://openregister.de/keys
# Enables Handelsregister research integration via MCP
OPENREGISTER_API_KEY=sk_live_xxxxx
